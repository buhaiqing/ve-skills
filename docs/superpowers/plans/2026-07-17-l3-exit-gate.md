# Plan: L3 Exit Gate — CI Enforcement of Trace + Safety Invariants (P5/P7)

> **Parent spec:** `docs/superpowers/specs/2026-07-17-l3-exit-gate-design.md`
> **Companion task cards:** `docs/l2-to-l3-tasks` P5 / P7
> **Status:** ready to implement (Spec approved). Implement after Spec sign-off.

---

## 0. Scope

Close the two remaining partial L3 DoD items by (a) fixing a real coverage bug in `vet check trace` (it ignores the actual runtime `gcl-trace-*.json` files) and (b) wiring `vet check trace` + `vet check policyguard` into CI. No new policy logic.

**Out of scope:** L4 features (T09–T16); any change to `scoreDecision` / `policyguard` invariants; `--confirmed-by` (shipped in `9d9e03c`).

---

## 1. Milestones

### M1 — Fix trace glob (`cmd/vet/check.go`)
**File:** `cmd/vet/check.go` `traceCheck`

- Remove the `strings.HasPrefix(entry.Name(), "incident-trace-")` skip.
- Instead check both `gcl-trace-` and `incident-trace-` prefixes (runtime + agent traces share schema).
- Keep `.json` + non-dir filter.

### M2 — Add trace regression test
**File:** `cmd/vet/internal/check/trace/trace_test.go`

- Add `TestCheck_GclTraceMissingRequestID`: write a `gcl-trace-*.json` (matching runtime `PersistTrace` shape) with a ve_call missing `request_id` into a temp `audit-results/`, assert `trace.Check` returns error mentioning `request_id`.
- Confirms the runtime-naming path is covered (the bug being fixed).

### M3 — Wire CI (` .github/workflows/validate.yml`)
**File:** `.github/workflows/validate.yml`

- After `vet gcl gate --skip-incident-loop`, add:
  - `vet check trace --root .` (P5)
  - `vet check policyguard --root .` (P7)

### M4 — Mark L3 DoD
**File:** `docs/l2-to-l3-plan.md`

- §6 L3 DoD: P5 ✅, P7 ✅ (set after CI locally verified green).

---

## 2. Function-level / command contract

| Spec component | Code anchor (authoritative) |
|----------------|------------------------------|
| Trace glob fix | `cmd/vet/check.go` `traceCheck` (~L228 prefix filter) |
| request_id enforcement | `cmd/vet/internal/check/trace/trace.go` `Check` (L67–71) — already correct, just needs to be reached |
| policyguard CI | `cmd/vet/check.go` `case "policyguard"` (L72) — already wired to fixture |
| CI steps | `.github/workflows/validate.yml` L49 onward |

> No new functions. M1 is a ~3-line filter change; M3 is 2 YAML lines.

---

## 3. DoD

```
✅ 1. `vet check trace` scans gcl-trace-*.json (runtime traces)
✅ 2. missing request_id in a gcl-trace-*.json → exit 1
✅ 3. new unit test covers gcl-trace-*.json request_id path
✅ 4. validate.yml runs `vet check trace` + `vet check policyguard`
✅ 5. forced regression makes CI red (verified locally by temp bad trace)
✅ 6. go build + go vet + go test ./... clean
✅ 7. l2-to-l3-plan.md §6 P5/P7 marked ✅
```

---

## 4. Verification

```bash
cd cmd/vet
go build ./... && go vet ./... && go test ./...
# local CI simulation
../bin/vet check trace --root /tmp/sample-repo
../bin/vet check policyguard --root .
# regression probe: drop a gcl-trace-*.json w/ missing request_id into audit-results/, expect exit 1
```

---

## 5. Three-Way Consistency Anchor

- **Spec ↔ Plan:** DoD (§3) == spec Success Criteria. ✅ aligned.
- **Plan ↔ Code:** anchors (§2) point to existing functions; M1/M3 are edits, not new symbols. ✅ aligned.
- **Spec ↔ Code:** after M1, `vet check trace` reaches `gcl-trace-*.json` → `trace.Check` enforces `request_id`. ✅ aligned.
